import type { NextPage } from "next";

const Home: NextPage = () => {
  return (
    <main style={{ padding: 24 }}>
      <h1>template_projet — Web</h1>
      <p>Next.js (TS) prêt. Backend: <code>http://localhost:8000</code></p>
    </main>
  );
};
export default Home;
